<!DOCTYPE HTML>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <meta http-equiv="x-ua-compatible" content="ie=edge" />
  <title>{% block title %}STUDI FITNESS | ECF 2022{% endblock %}</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
  <!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css" integrity="sha512-1sCRPdkRXhBV2PBLUdRb4tMg1w2YPf37qatUFeS7zlBy7jJI8Lf4VHwWfZZfpXtYSLy85pkm9GaYVYMfw5BC1A==" crossorigin="anonymous" referrerpolicy="no-referrer" />  <!-- Google Fonts Roboto -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
  <!--DM Serif Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
  <!-- Boostrap -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.9.2/umd/popper.min.js" integrity="sha512-2rNj2KJ+D8s1ceNasTIex6z4HWyOnEYLVC3FigGOmyQCZc2eBXKgOxQmo3oKLHyfcj53uz4QMsRCWNbLd32Q1g==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
  
  {# WebPack Encore Scripts (include My own CSS Styles on App.CSS + JS Scripts on App.js (assets/styles)) #}
  {% block stylesheets %}
    {{ encore_entry_link_tags('app')}}
  {% endblock %}

  {% block javascripts %}
    {{ encore_entry_script_tags('app')}}

  {% endblock %}
</head>

<body class="position-relative">

<form method="post">

    <section class="section-login">
        <div class="container py-5 h-100 position-relative">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col col-xl-10">
                    <div class="card" style="border-radius: 1rem;">
                        <div class="row g-0">
                            <div class="col-md-6 col-lg-5 d-none d-md-block left-picture"></div>
                            <div class="col-md-6 col-lg-7 d-flex align-items-center">
                                <div class="card-body p-4 p-lg-5 text-black">

                                    <form>
                                    <div class="logoTitleWrapper row d-flex justify-content-center">
                                        <img class="logo-image" src="{{ asset('build/images/STUDI-FITNESS-LOGO.jpg') }}" alt="profile-picture">
                                        <div class="col-xl-12 d-flex justify-content-center align-items-center mb-3 pb-1 fs-3 login-title">
                                        {# <i class="fa fa-dumbbell mr-2" style="color: #667eea";></i> #}
                                        <span class="col h1 fw-bold mb-0 text-center">STUDI FITNESS GROUP</span>
                                        </div>
                                    </div>


                                    <h5 class="fw-normal mb-3 pb-3 text-center" style="letter-spacing: 1px;">Connectez-vous à votre compte</h5>

                                    {% if error %}
                                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                    {% endif %}

                                    {% if app.user %}
                                        <div class="mb-3">
                                            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
                                        </div>
                                    {% endif %}

                                    {% for message in app.flashes('notice') %}
                                        <div class="alert alert-info alert-dismissible fade show m-0" role="alert">
                                            {{ message }}
                                        </div>
                                    {% endfor %}

                                    <div class="form-outline mb-4">
                                        <label for="inputEmail" class="form-label">Votre email</label>
                                        <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" placeholder="Votre adresse email" required autofocus>
                                    </div>

                                    <div class="form-outline mb-4">
                                        <label for="inputPassword" class="form-label">Votre mot de passe</label>
                                        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" placeholder="Votre mot de passe" required>
                                    </div>

                                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                                    <div class="checkbox mb-3">
                                        <label>
                                            <input type="checkbox" name="_remember_me" class="mx-1">Se souvenir de moi
                                        </label>
                                    </div>
                                    

                                    <div class="pt-1 mb-2">
                                        <button class="btn btn-dark btn-lg btn-block" type="submit">Se connecter</button>
                                    </div>

                                    <a class="text-muted" href="{{ path('app_reset_password') }}">Mot de passe oublié ?</a>
                                    
                                    </form>

                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="copyright position-absolute d-flex justify-content-center align-items-center w-100">
               <small><a class="text-decoration-none text-white p-4" href="http://www.nicolasbarthes.com">handmade by NB | 2022 ©</a></small>
            </div>
        </div>
    </section>
</form>


</body>
</html>